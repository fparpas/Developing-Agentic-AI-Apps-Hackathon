# =============================================================================
# Secure MCP Weather Solution - Environment Configuration
# Challenge 09 Solution
# =============================================================================

# -----------------------------------------------------------------------------
# Server Configuration (secure_weather_server.py)
# -----------------------------------------------------------------------------

# API Key for server authentication
# Clients must provide this in the X-API-Key header
API_KEY=SuperSecureSecretUsedAsApiKey1

# Server port (optional, defaults to 5000)
PORT=5000

# -----------------------------------------------------------------------------
# Client Configuration (secure_weather_client.py)
# -----------------------------------------------------------------------------

# Azure OpenAI Configuration
# Your Azure OpenAI resource endpoint (include the trailing slash)
AZURE_OPENAI_ENDPOINT=https://your-resource-name.openai.azure.com/

# Your Azure OpenAI API key
AZURE_OPENAI_API_KEY=your_azure_openai_api_key_here

# Your deployed model name (e.g., gpt-4, gpt-4o, gpt-35-turbo)
AZURE_OPENAI_DEPLOYMENT_NAME=gpt-4

# API version (optional, defaults to 2024-10-21)
AZURE_OPENAI_API_VERSION=2024-10-21

# MCP Server Configuration
# Use local MCP server (stdio) or remote MCP server (SSE with API key)
USE_LOCAL_MCP=false

# Remote MCP server endpoint (should match where your server is running)
MCP_SERVER_URL=http://localhost:5000

# -----------------------------------------------------------------------------
# Usage Instructions
# -----------------------------------------------------------------------------
# 1. Copy this file to .env
# 2. Replace the placeholder values with your actual credentials
# 3. Never commit .env to version control (it's in .gitignore)
# 4. For production, use Azure Key Vault or similar secret management
# 5. Ensure API_KEY matches between server and client configurations
